image: zoowii/hxcore_hxbuild:latest

before_script:
    - echo hello
    - git submodule update --init --recursive

stages:
  - build
  - test

build:
  stage: build
  script:
    - cmake .
    - make

test:
  script:
    - echo hello
