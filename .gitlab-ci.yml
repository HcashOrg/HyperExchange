image: zoowii/hxcore_hxbuild:latest

before_script:
    - echo hello
    - git submodule update --init --recursive

stages:
  - build
  - test

build:
  stage: build
  script:
    - cmake .
    - cd programs/witness_node && make -j2

test:
  script:
    - echo hello
